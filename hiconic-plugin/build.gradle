plugins {
    // Apply the java-library plugin for API and implementation separation.
    id 'java-library'
    // Apply the java-gradle-plugin plugin because this artifact is a gradle plugin
    id 'java-gradle-plugin'
    // Apply the eclipse plugin because currently we use the eclipse IDE
    id 'eclipse'
    id 'maven-publish'
}

group = 'hiconic.devrock.gradle'
version = '1.0.1'

repositories {
    mavenLocal()

    // Use Maven Central for resolving dependencies.
    mavenCentral()
}

dependencies {
    // Use JUnit test framework.
    testImplementation libs.junit

    implementation gradleApi()
    testImplementation gradleTestKit()
    
    implementation 'org.apache.flink:flink-shaded-asm-9:9.9.1-21.0'
}

// Apply a specific Java toolchain to ease working on different environments.
java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(25)
    }
}

// compile with the installed JDK 25, but target 24, which is the first release with the
// Class-File API. So a gradle daemon on java 24 can load this plugin too.
tasks.withType(JavaCompile).configureEach {
    options.release = 24
}

gradlePlugin {
    plugins {
        myPlugin {
            //id = 'hiconic.gradle.plugin:hiconic-plugin'
            id = 'hiconic'
            implementationClass = 'hiconic.gradle.plugin.HiconicPlugin'
        }
    }
}